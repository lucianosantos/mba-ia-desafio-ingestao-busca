# Desafio MBA Engenharia de Software com IA - Full Cycle

python -m venv venv

To activate the venv:

In Git Bash, execute source venv/Scripts/activate
In power Shell, execute venv/Scripts/activate.ps1

Install dependencies

pip install -r requirements.txt

Init DB in Docker

docker compose up -d